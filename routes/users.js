const router = require('express').Router();
const { auth } = require('../server');

router.route('/signup')
	.post((req, res) => {
		// TODO: implement
	});

router.route('/signin')
	.post((req, res) => {
		// TODO: implement
	});

module.exports = router;